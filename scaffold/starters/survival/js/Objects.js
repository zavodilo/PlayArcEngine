// Objects.js — the survival starter: the hero only; enemies are spawned by Game.js
// through the semantic Scene API (validated against js/SceneSchema.js).
const LOCATION_OBJECTS = [
    { name: 'hero', model: 'assets/models/character.glb', kind: 'actor', x: 1024, y: 1024, h: 0, rot: [0, 0, 0], scale: [0.25, 0.25, 0.25], clip: 'idle' },
];
